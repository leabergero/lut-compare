__pycache__/
*.pyc
.venv/
venv/

# artefactos de compilacion del instalador
build/
dist/
*.egg-info/

# presets personales del usuario (los .xmp de Lightroom pueden ser
# contenido comercial de terceros y no deben redistribuirse)
presets/*.xmp

# salidas y estado local
editadas/
.lut_compare.json

# carpetas de staging intermedias al generar los instaladores
# (los .deb/.msi/.pkg finales en installers/ SI se versionan)
installers/.build-deb/
installers/.build-msi/
installers/.build-pkg/
